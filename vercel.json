{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "HUSKY=0 npm install --legacy-peer-deps",
  "regions": ["iad1"],
  "git": {
    "deploymentEnabled": {
      "main": true,
      "develop": true
    }
  }
}
